# python -m plugins.cover *COMMAND*

Generating covers.

## make-grid

Create a grid cover from existing covers.

Args:

* `covers` (positional): list of paths to existing covers.
* `--output`: path to the output file.